import React from 'react';

const MapView: React.FC = () => {
    return (
        <div>
            <h2>Map View</h2>
            {/* Integrate Google Maps API here */}
        </div>
    );
};

export default MapView;