# Example env for frontend dev
# Use MSW (mocks) in dev: set to true (default behavior when not set).
VITE_USE_MSW=true
# API base URL. Leave empty when using MSW (so frontend will use relative paths).
# For real backend use: http://localhost:8081
VITE_API_URL=http://localhost:8081
